# Source Manifest — EduNova V1.0 Stable

Final source dirakit dari baseline Phase 03 v3.1 dan overlay Phase 04–10, termasuk performance patch sebelum final Phase 10.

- Backend PHP files: 147
- Frontend TS/TSX files: 71
- Database migration files: 38

Dependency directories (`vendor`, `node_modules`) sengaja tidak dimasukkan. Docker production build akan memasangnya dari `composer.json` dan `package.json`.
